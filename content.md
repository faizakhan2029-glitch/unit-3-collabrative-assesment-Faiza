# short response Questions.

If im collabrating with my classmate the first thing i need to do is pull request because it gives the other person (your partner) access to your branch.

 devolpers use branches to diverge from the main branch to continue working without affecting the main branch  diverging means to split from the main branch into a different branch.

 git is a command line test that allows you to track and merge changes to your code version control of the center, github is a web based platform to store repos in the cloud and collabrate with other devolpers,user-friendly these two work together by github being the main source and git being a command line inside the github main source.

 The sequence is wrong because git add . goes first then comes git commit and then git push so the sequence was just placed in the wrong order.
 